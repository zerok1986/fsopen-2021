{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "A training purposes project",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "test:watch": "NODE_ENV=test jest --verbose --watch"
  },
  "author": "zerok1986",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.2.0"
  },
  "devDependencies": {
    "jest": "^27.5.0",
    "nodemon": "^2.0.15"
  },
  "jest": {
   "testEnvironment": "node"
 }
}
